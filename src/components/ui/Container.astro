---
const { class: className = "", contain = true } = Astro.props;
const classes = `${className} ${contain ? "container mx-auto px-4 sm:px-6 lg:px-8 py-16 lg:py-24" : ""}`;
---

<div class={classes}>
  <slot />
</div>
